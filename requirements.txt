# The pure-Python examples/ need NOTHING beyond the standard library.
# These are only for running the full back-end + front-end stack (backend/app.py).
fastapi>=0.110
uvicorn>=0.29
# pydantic is pulled in by fastapi
